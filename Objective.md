# Building a domain-specific "Ask My Docs" system with hybrid retrieval (BM25 + vector search), cross-encoder reranking, citation enforcement, and a CI-gated evaluation pipeline

## **Phase 1**
>Build a basic RAG pipeline using an embedding model (bi-encoder), a vector database like ChromaDB, and cosine similarity for top-k retrieval, followed by LLM-based answer generation.

